import 'package:flutter/material.dart';

class CircuitScreen extends StatelessWidget {
  const CircuitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Circuit Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
